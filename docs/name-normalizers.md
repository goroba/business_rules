# Name normalizers

Guide coming soon. Name normalizers convert registered entry names to a canonical form (e.g. `userAge` → `user_age`). Pass a `NameNormalizer` to `Engine` or `Context`, or subclass `NameNormalizer` to define your own.
